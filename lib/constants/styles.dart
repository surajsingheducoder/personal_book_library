import 'package:flutter/material.dart';

class Styles {
  static TextStyle headingStyle(double screenHeight) {
    return TextStyle(
      fontWeight: FontWeight.bold,
      fontSize: screenHeight / 30,
    );
  }
}
